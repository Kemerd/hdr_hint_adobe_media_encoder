// ---------------------------------------------------------------------------
// PathIcons.h - vector icons drawn from primitives (no bitmap assets).
// ---------------------------------------------------------------------------
#pragma once

namespace hh::ui {

enum class IconId {
    None,
    Close, Minimize, Maximize, Restore,
    ChevronDown, ChevronRight, Check,
    Play, Pause, Stop, Refresh,
    Folder, Eye, Trash, Copy, ExternalLink,
    Plus, Minus, Warning, Info, Error, Success,
    FilmFrame, AppMark, Search, Gear, Book, Dock, Undock, Pin,
};

} // namespace hh::ui
